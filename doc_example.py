def mystery_function(a, b):
    """
    This function adds the two parameters together.

    It *will* throw an exception if either parameter
    is None, and also if there is no way to add them
    together.

    Parameters
    ----------
    a : Any
        The addend
    b : Any
        the augend

    Returns
    -------
    Any
        The result of a + b
    """
    return a + b